/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HookResponse from './HookResponse';
import WebhookConfig from './WebhookConfig';

/**
 * The Hook model module.
 * @module model/Hook
 * @version 1.1.4
 */
class Hook {
    /**
     * Constructs a new <code>Hook</code>.
     * Webhooks for repositories.
     * @alias module:model/Hook
     * @param type {String} 
     * @param id {Number} Unique identifier of the webhook.
     * @param name {String} The name of a valid service, use 'web' for a webhook.
     * @param active {Boolean} Determines whether the hook is actually triggered on pushes.
     * @param events {Array.<String>} Determines what events the hook is triggered for. Default: ['push'].
     * @param config {module:model/WebhookConfig} 
     * @param updatedAt {Date} 
     * @param createdAt {Date} 
     * @param url {String} 
     * @param testUrl {String} 
     * @param pingUrl {String} 
     * @param lastResponse {module:model/HookResponse} 
     */
    constructor(type, id, name, active, events, config, updatedAt, createdAt, url, testUrl, pingUrl, lastResponse) { 
        
        Hook.initialize(this, type, id, name, active, events, config, updatedAt, createdAt, url, testUrl, pingUrl, lastResponse);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, id, name, active, events, config, updatedAt, createdAt, url, testUrl, pingUrl, lastResponse) { 
        obj['type'] = type;
        obj['id'] = id;
        obj['name'] = name;
        obj['active'] = active;
        obj['events'] = events;
        obj['config'] = config;
        obj['updated_at'] = updatedAt;
        obj['created_at'] = createdAt;
        obj['url'] = url;
        obj['test_url'] = testUrl;
        obj['ping_url'] = pingUrl;
        obj['last_response'] = lastResponse;
    }

    /**
     * Constructs a <code>Hook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Hook} obj Optional instance to populate.
     * @return {module:model/Hook} The populated <code>Hook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Hook();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('events')) {
                obj['events'] = ApiClient.convertToType(data['events'], ['String']);
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = WebhookConfig.constructFromObject(data['config']);
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('test_url')) {
                obj['test_url'] = ApiClient.convertToType(data['test_url'], 'String');
            }
            if (data.hasOwnProperty('ping_url')) {
                obj['ping_url'] = ApiClient.convertToType(data['ping_url'], 'String');
            }
            if (data.hasOwnProperty('deliveries_url')) {
                obj['deliveries_url'] = ApiClient.convertToType(data['deliveries_url'], 'String');
            }
            if (data.hasOwnProperty('last_response')) {
                obj['last_response'] = HookResponse.constructFromObject(data['last_response']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Hook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Hook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Hook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['events'])) {
            throw new Error("Expected the field `events` to be an array in the JSON data but got " + data['events']);
        }
        // validate the optional field `config`
        if (data['config']) { // data not null
          WebhookConfig.validateJSON(data['config']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // ensure the json data is a string
        if (data['test_url'] && !(typeof data['test_url'] === 'string' || data['test_url'] instanceof String)) {
            throw new Error("Expected the field `test_url` to be a primitive type in the JSON string but got " + data['test_url']);
        }
        // ensure the json data is a string
        if (data['ping_url'] && !(typeof data['ping_url'] === 'string' || data['ping_url'] instanceof String)) {
            throw new Error("Expected the field `ping_url` to be a primitive type in the JSON string but got " + data['ping_url']);
        }
        // ensure the json data is a string
        if (data['deliveries_url'] && !(typeof data['deliveries_url'] === 'string' || data['deliveries_url'] instanceof String)) {
            throw new Error("Expected the field `deliveries_url` to be a primitive type in the JSON string but got " + data['deliveries_url']);
        }
        // validate the optional field `last_response`
        if (data['last_response']) { // data not null
          HookResponse.validateJSON(data['last_response']);
        }

        return true;
    }


}

Hook.RequiredProperties = ["type", "id", "name", "active", "events", "config", "updated_at", "created_at", "url", "test_url", "ping_url", "last_response"];

/**
 * @member {String} type
 */
Hook.prototype['type'] = undefined;

/**
 * Unique identifier of the webhook.
 * @member {Number} id
 */
Hook.prototype['id'] = undefined;

/**
 * The name of a valid service, use 'web' for a webhook.
 * @member {String} name
 */
Hook.prototype['name'] = undefined;

/**
 * Determines whether the hook is actually triggered on pushes.
 * @member {Boolean} active
 */
Hook.prototype['active'] = undefined;

/**
 * Determines what events the hook is triggered for. Default: ['push'].
 * @member {Array.<String>} events
 */
Hook.prototype['events'] = undefined;

/**
 * @member {module:model/WebhookConfig} config
 */
Hook.prototype['config'] = undefined;

/**
 * @member {Date} updated_at
 */
Hook.prototype['updated_at'] = undefined;

/**
 * @member {Date} created_at
 */
Hook.prototype['created_at'] = undefined;

/**
 * @member {String} url
 */
Hook.prototype['url'] = undefined;

/**
 * @member {String} test_url
 */
Hook.prototype['test_url'] = undefined;

/**
 * @member {String} ping_url
 */
Hook.prototype['ping_url'] = undefined;

/**
 * @member {String} deliveries_url
 */
Hook.prototype['deliveries_url'] = undefined;

/**
 * @member {module:model/HookResponse} last_response
 */
Hook.prototype['last_response'] = undefined;






export default Hook;

