/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CodeScanningVariantAnalysisRepository from './CodeScanningVariantAnalysisRepository';

/**
 * The CodeScanningVariantAnalysisSkippedRepoGroup model module.
 * @module model/CodeScanningVariantAnalysisSkippedRepoGroup
 * @version 1.1.4
 */
class CodeScanningVariantAnalysisSkippedRepoGroup {
    /**
     * Constructs a new <code>CodeScanningVariantAnalysisSkippedRepoGroup</code>.
     * @alias module:model/CodeScanningVariantAnalysisSkippedRepoGroup
     * @param repositoryCount {Number} The total number of repositories that were skipped for this reason.
     * @param repositories {Array.<module:model/CodeScanningVariantAnalysisRepository>} A list of repositories that were skipped. This list may not include all repositories that were skipped. This is only available when the repository was found and the user has access to it.
     */
    constructor(repositoryCount, repositories) { 
        
        CodeScanningVariantAnalysisSkippedRepoGroup.initialize(this, repositoryCount, repositories);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, repositoryCount, repositories) { 
        obj['repository_count'] = repositoryCount;
        obj['repositories'] = repositories;
    }

    /**
     * Constructs a <code>CodeScanningVariantAnalysisSkippedRepoGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CodeScanningVariantAnalysisSkippedRepoGroup} obj Optional instance to populate.
     * @return {module:model/CodeScanningVariantAnalysisSkippedRepoGroup} The populated <code>CodeScanningVariantAnalysisSkippedRepoGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CodeScanningVariantAnalysisSkippedRepoGroup();

            if (data.hasOwnProperty('repository_count')) {
                obj['repository_count'] = ApiClient.convertToType(data['repository_count'], 'Number');
            }
            if (data.hasOwnProperty('repositories')) {
                obj['repositories'] = ApiClient.convertToType(data['repositories'], [CodeScanningVariantAnalysisRepository]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CodeScanningVariantAnalysisSkippedRepoGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CodeScanningVariantAnalysisSkippedRepoGroup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CodeScanningVariantAnalysisSkippedRepoGroup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['repositories']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['repositories'])) {
                throw new Error("Expected the field `repositories` to be an array in the JSON data but got " + data['repositories']);
            }
            // validate the optional field `repositories` (array)
            for (const item of data['repositories']) {
                CodeScanningVariantAnalysisRepository.validateJSON(item);
            };
        }

        return true;
    }


}

CodeScanningVariantAnalysisSkippedRepoGroup.RequiredProperties = ["repository_count", "repositories"];

/**
 * The total number of repositories that were skipped for this reason.
 * @member {Number} repository_count
 */
CodeScanningVariantAnalysisSkippedRepoGroup.prototype['repository_count'] = undefined;

/**
 * A list of repositories that were skipped. This list may not include all repositories that were skipped. This is only available when the repository was found and the user has access to it.
 * @member {Array.<module:model/CodeScanningVariantAnalysisRepository>} repositories
 */
CodeScanningVariantAnalysisSkippedRepoGroup.prototype['repositories'] = undefined;






export default CodeScanningVariantAnalysisSkippedRepoGroup;

