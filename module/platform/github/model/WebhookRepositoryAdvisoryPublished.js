/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnterpriseWebhooks from './EnterpriseWebhooks';
import OrganizationSimpleWebhooks from './OrganizationSimpleWebhooks';
import RepositoryAdvisory from './RepositoryAdvisory';
import RepositoryWebhooks from './RepositoryWebhooks';
import SimpleInstallation from './SimpleInstallation';
import SimpleUserWebhooks from './SimpleUserWebhooks';

/**
 * The WebhookRepositoryAdvisoryPublished model module.
 * @module model/WebhookRepositoryAdvisoryPublished
 * @version 1.1.4
 */
class WebhookRepositoryAdvisoryPublished {
    /**
     * Constructs a new <code>WebhookRepositoryAdvisoryPublished</code>.
     * @alias module:model/WebhookRepositoryAdvisoryPublished
     * @param action {module:model/WebhookRepositoryAdvisoryPublished.ActionEnum} 
     * @param repository {module:model/RepositoryWebhooks} 
     * @param repositoryAdvisory {module:model/RepositoryAdvisory} 
     */
    constructor(action, repository, repositoryAdvisory) { 
        
        WebhookRepositoryAdvisoryPublished.initialize(this, action, repository, repositoryAdvisory);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action, repository, repositoryAdvisory) { 
        obj['action'] = action;
        obj['repository'] = repository;
        obj['repository_advisory'] = repositoryAdvisory;
    }

    /**
     * Constructs a <code>WebhookRepositoryAdvisoryPublished</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebhookRepositoryAdvisoryPublished} obj Optional instance to populate.
     * @return {module:model/WebhookRepositoryAdvisoryPublished} The populated <code>WebhookRepositoryAdvisoryPublished</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebhookRepositoryAdvisoryPublished();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('enterprise')) {
                obj['enterprise'] = EnterpriseWebhooks.constructFromObject(data['enterprise']);
            }
            if (data.hasOwnProperty('installation')) {
                obj['installation'] = SimpleInstallation.constructFromObject(data['installation']);
            }
            if (data.hasOwnProperty('organization')) {
                obj['organization'] = OrganizationSimpleWebhooks.constructFromObject(data['organization']);
            }
            if (data.hasOwnProperty('repository')) {
                obj['repository'] = RepositoryWebhooks.constructFromObject(data['repository']);
            }
            if (data.hasOwnProperty('repository_advisory')) {
                obj['repository_advisory'] = RepositoryAdvisory.constructFromObject(data['repository_advisory']);
            }
            if (data.hasOwnProperty('sender')) {
                obj['sender'] = SimpleUserWebhooks.constructFromObject(data['sender']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebhookRepositoryAdvisoryPublished</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebhookRepositoryAdvisoryPublished</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebhookRepositoryAdvisoryPublished.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // validate the optional field `enterprise`
        if (data['enterprise']) { // data not null
          EnterpriseWebhooks.validateJSON(data['enterprise']);
        }
        // validate the optional field `installation`
        if (data['installation']) { // data not null
          SimpleInstallation.validateJSON(data['installation']);
        }
        // validate the optional field `organization`
        if (data['organization']) { // data not null
          OrganizationSimpleWebhooks.validateJSON(data['organization']);
        }
        // validate the optional field `repository`
        if (data['repository']) { // data not null
          RepositoryWebhooks.validateJSON(data['repository']);
        }
        // validate the optional field `repository_advisory`
        if (data['repository_advisory']) { // data not null
          RepositoryAdvisory.validateJSON(data['repository_advisory']);
        }
        // validate the optional field `sender`
        if (data['sender']) { // data not null
          SimpleUserWebhooks.validateJSON(data['sender']);
        }

        return true;
    }


}

WebhookRepositoryAdvisoryPublished.RequiredProperties = ["action", "repository", "repository_advisory"];

/**
 * @member {module:model/WebhookRepositoryAdvisoryPublished.ActionEnum} action
 */
WebhookRepositoryAdvisoryPublished.prototype['action'] = undefined;

/**
 * @member {module:model/EnterpriseWebhooks} enterprise
 */
WebhookRepositoryAdvisoryPublished.prototype['enterprise'] = undefined;

/**
 * @member {module:model/SimpleInstallation} installation
 */
WebhookRepositoryAdvisoryPublished.prototype['installation'] = undefined;

/**
 * @member {module:model/OrganizationSimpleWebhooks} organization
 */
WebhookRepositoryAdvisoryPublished.prototype['organization'] = undefined;

/**
 * @member {module:model/RepositoryWebhooks} repository
 */
WebhookRepositoryAdvisoryPublished.prototype['repository'] = undefined;

/**
 * @member {module:model/RepositoryAdvisory} repository_advisory
 */
WebhookRepositoryAdvisoryPublished.prototype['repository_advisory'] = undefined;

/**
 * @member {module:model/SimpleUserWebhooks} sender
 */
WebhookRepositoryAdvisoryPublished.prototype['sender'] = undefined;





/**
 * Allowed values for the <code>action</code> property.
 * @enum {String}
 * @readonly
 */
WebhookRepositoryAdvisoryPublished['ActionEnum'] = {

    /**
     * value: "published"
     * @const
     */
    "published": "published"
};



export default WebhookRepositoryAdvisoryPublished;

