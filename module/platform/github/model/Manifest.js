/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Dependency from './Dependency';
import ManifestFile from './ManifestFile';
import Metadata1 from './Metadata1';

/**
 * The Manifest model module.
 * @module model/Manifest
 * @version 1.1.4
 */
class Manifest {
    /**
     * Constructs a new <code>Manifest</code>.
     * @alias module:model/Manifest
     * @param name {String} The name of the manifest.
     */
    constructor(name) { 
        
        Manifest.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>Manifest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Manifest} obj Optional instance to populate.
     * @return {module:model/Manifest} The populated <code>Manifest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Manifest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('file')) {
                obj['file'] = ManifestFile.constructFromObject(data['file']);
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], {'String': Metadata1});
            }
            if (data.hasOwnProperty('resolved')) {
                obj['resolved'] = ApiClient.convertToType(data['resolved'], {'String': Dependency});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Manifest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Manifest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Manifest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // validate the optional field `file`
        if (data['file']) { // data not null
          ManifestFile.validateJSON(data['file']);
        }

        return true;
    }


}

Manifest.RequiredProperties = ["name"];

/**
 * The name of the manifest.
 * @member {String} name
 */
Manifest.prototype['name'] = undefined;

/**
 * @member {module:model/ManifestFile} file
 */
Manifest.prototype['file'] = undefined;

/**
 * User-defined metadata to store domain-specific information limited to 8 keys with scalar values.
 * @member {Object.<String, module:model/Metadata1>} metadata
 */
Manifest.prototype['metadata'] = undefined;

/**
 * A collection of resolved package dependencies.
 * @member {Object.<String, module:model/Dependency>} resolved
 */
Manifest.prototype['resolved'] = undefined;






export default Manifest;

