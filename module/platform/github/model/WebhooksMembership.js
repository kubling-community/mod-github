/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import User from './User';

/**
 * The WebhooksMembership model module.
 * @module model/WebhooksMembership
 * @version 1.1.4
 */
class WebhooksMembership {
    /**
     * Constructs a new <code>WebhooksMembership</code>.
     * The membership between the user and the organization. Not present when the action is &#x60;member_invited&#x60;.
     * @alias module:model/WebhooksMembership
     * @param organizationUrl {String} 
     * @param role {String} 
     * @param state {String} 
     * @param url {String} 
     * @param user {module:model/User} 
     */
    constructor(organizationUrl, role, state, url, user) { 
        
        WebhooksMembership.initialize(this, organizationUrl, role, state, url, user);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, organizationUrl, role, state, url, user) { 
        obj['organization_url'] = organizationUrl;
        obj['role'] = role;
        obj['state'] = state;
        obj['url'] = url;
        obj['user'] = user;
    }

    /**
     * Constructs a <code>WebhooksMembership</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebhooksMembership} obj Optional instance to populate.
     * @return {module:model/WebhooksMembership} The populated <code>WebhooksMembership</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebhooksMembership();

            if (data.hasOwnProperty('organization_url')) {
                obj['organization_url'] = ApiClient.convertToType(data['organization_url'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = User.constructFromObject(data['user']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebhooksMembership</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebhooksMembership</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebhooksMembership.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['organization_url'] && !(typeof data['organization_url'] === 'string' || data['organization_url'] instanceof String)) {
            throw new Error("Expected the field `organization_url` to be a primitive type in the JSON string but got " + data['organization_url']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // validate the optional field `user`
        if (data['user']) { // data not null
          User.validateJSON(data['user']);
        }

        return true;
    }


}

WebhooksMembership.RequiredProperties = ["organization_url", "role", "state", "url", "user"];

/**
 * @member {String} organization_url
 */
WebhooksMembership.prototype['organization_url'] = undefined;

/**
 * @member {String} role
 */
WebhooksMembership.prototype['role'] = undefined;

/**
 * @member {String} state
 */
WebhooksMembership.prototype['state'] = undefined;

/**
 * @member {String} url
 */
WebhooksMembership.prototype['url'] = undefined;

/**
 * @member {module:model/User} user
 */
WebhooksMembership.prototype['user'] = undefined;






export default WebhooksMembership;

