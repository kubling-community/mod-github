/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CustomDeploymentRuleApp model module.
 * @module model/CustomDeploymentRuleApp
 * @version 1.1.4
 */
class CustomDeploymentRuleApp {
    /**
     * Constructs a new <code>CustomDeploymentRuleApp</code>.
     * A GitHub App that is providing a custom deployment protection rule.
     * @alias module:model/CustomDeploymentRuleApp
     * @param id {Number} The unique identifier of the deployment protection rule integration.
     * @param slug {String} The slugified name of the deployment protection rule integration.
     * @param integrationUrl {String} The URL for the endpoint to get details about the app.
     * @param nodeId {String} The node ID for the deployment protection rule integration.
     */
    constructor(id, slug, integrationUrl, nodeId) { 
        
        CustomDeploymentRuleApp.initialize(this, id, slug, integrationUrl, nodeId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, slug, integrationUrl, nodeId) { 
        obj['id'] = id;
        obj['slug'] = slug;
        obj['integration_url'] = integrationUrl;
        obj['node_id'] = nodeId;
    }

    /**
     * Constructs a <code>CustomDeploymentRuleApp</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomDeploymentRuleApp} obj Optional instance to populate.
     * @return {module:model/CustomDeploymentRuleApp} The populated <code>CustomDeploymentRuleApp</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomDeploymentRuleApp();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
            if (data.hasOwnProperty('integration_url')) {
                obj['integration_url'] = ApiClient.convertToType(data['integration_url'], 'String');
            }
            if (data.hasOwnProperty('node_id')) {
                obj['node_id'] = ApiClient.convertToType(data['node_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomDeploymentRuleApp</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomDeploymentRuleApp</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CustomDeploymentRuleApp.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }
        // ensure the json data is a string
        if (data['integration_url'] && !(typeof data['integration_url'] === 'string' || data['integration_url'] instanceof String)) {
            throw new Error("Expected the field `integration_url` to be a primitive type in the JSON string but got " + data['integration_url']);
        }
        // ensure the json data is a string
        if (data['node_id'] && !(typeof data['node_id'] === 'string' || data['node_id'] instanceof String)) {
            throw new Error("Expected the field `node_id` to be a primitive type in the JSON string but got " + data['node_id']);
        }

        return true;
    }


}

CustomDeploymentRuleApp.RequiredProperties = ["id", "slug", "integration_url", "node_id"];

/**
 * The unique identifier of the deployment protection rule integration.
 * @member {Number} id
 */
CustomDeploymentRuleApp.prototype['id'] = undefined;

/**
 * The slugified name of the deployment protection rule integration.
 * @member {String} slug
 */
CustomDeploymentRuleApp.prototype['slug'] = undefined;

/**
 * The URL for the endpoint to get details about the app.
 * @member {String} integration_url
 */
CustomDeploymentRuleApp.prototype['integration_url'] = undefined;

/**
 * The node ID for the deployment protection rule integration.
 * @member {String} node_id
 */
CustomDeploymentRuleApp.prototype['node_id'] = undefined;






export default CustomDeploymentRuleApp;

