/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WebhookGollumPagesInner model module.
 * @module model/WebhookGollumPagesInner
 * @version 1.1.4
 */
class WebhookGollumPagesInner {
    /**
     * Constructs a new <code>WebhookGollumPagesInner</code>.
     * @alias module:model/WebhookGollumPagesInner
     * @param action {module:model/WebhookGollumPagesInner.ActionEnum} The action that was performed on the page. Can be `created` or `edited`.
     * @param htmlUrl {String} Points to the HTML wiki page.
     * @param pageName {String} The name of the page.
     * @param sha {String} The latest commit SHA of the page.
     * @param summary {String} 
     * @param title {String} The current page title.
     */
    constructor(action, htmlUrl, pageName, sha, summary, title) { 
        
        WebhookGollumPagesInner.initialize(this, action, htmlUrl, pageName, sha, summary, title);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action, htmlUrl, pageName, sha, summary, title) { 
        obj['action'] = action;
        obj['html_url'] = htmlUrl;
        obj['page_name'] = pageName;
        obj['sha'] = sha;
        obj['summary'] = summary;
        obj['title'] = title;
    }

    /**
     * Constructs a <code>WebhookGollumPagesInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebhookGollumPagesInner} obj Optional instance to populate.
     * @return {module:model/WebhookGollumPagesInner} The populated <code>WebhookGollumPagesInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebhookGollumPagesInner();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('html_url')) {
                obj['html_url'] = ApiClient.convertToType(data['html_url'], 'String');
            }
            if (data.hasOwnProperty('page_name')) {
                obj['page_name'] = ApiClient.convertToType(data['page_name'], 'String');
            }
            if (data.hasOwnProperty('sha')) {
                obj['sha'] = ApiClient.convertToType(data['sha'], 'String');
            }
            if (data.hasOwnProperty('summary')) {
                obj['summary'] = ApiClient.convertToType(data['summary'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebhookGollumPagesInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebhookGollumPagesInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebhookGollumPagesInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // ensure the json data is a string
        if (data['html_url'] && !(typeof data['html_url'] === 'string' || data['html_url'] instanceof String)) {
            throw new Error("Expected the field `html_url` to be a primitive type in the JSON string but got " + data['html_url']);
        }
        // ensure the json data is a string
        if (data['page_name'] && !(typeof data['page_name'] === 'string' || data['page_name'] instanceof String)) {
            throw new Error("Expected the field `page_name` to be a primitive type in the JSON string but got " + data['page_name']);
        }
        // ensure the json data is a string
        if (data['sha'] && !(typeof data['sha'] === 'string' || data['sha'] instanceof String)) {
            throw new Error("Expected the field `sha` to be a primitive type in the JSON string but got " + data['sha']);
        }
        // ensure the json data is a string
        if (data['summary'] && !(typeof data['summary'] === 'string' || data['summary'] instanceof String)) {
            throw new Error("Expected the field `summary` to be a primitive type in the JSON string but got " + data['summary']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

WebhookGollumPagesInner.RequiredProperties = ["action", "html_url", "page_name", "sha", "summary", "title"];

/**
 * The action that was performed on the page. Can be `created` or `edited`.
 * @member {module:model/WebhookGollumPagesInner.ActionEnum} action
 */
WebhookGollumPagesInner.prototype['action'] = undefined;

/**
 * Points to the HTML wiki page.
 * @member {String} html_url
 */
WebhookGollumPagesInner.prototype['html_url'] = undefined;

/**
 * The name of the page.
 * @member {String} page_name
 */
WebhookGollumPagesInner.prototype['page_name'] = undefined;

/**
 * The latest commit SHA of the page.
 * @member {String} sha
 */
WebhookGollumPagesInner.prototype['sha'] = undefined;

/**
 * @member {String} summary
 */
WebhookGollumPagesInner.prototype['summary'] = undefined;

/**
 * The current page title.
 * @member {String} title
 */
WebhookGollumPagesInner.prototype['title'] = undefined;





/**
 * Allowed values for the <code>action</code> property.
 * @enum {String}
 * @readonly
 */
WebhookGollumPagesInner['ActionEnum'] = {

    /**
     * value: "created"
     * @const
     */
    "created": "created",

    /**
     * value: "edited"
     * @const
     */
    "edited": "edited"
};



export default WebhookGollumPagesInner;

