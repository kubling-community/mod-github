/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReposUpdateReleaseRequest model module.
 * @module model/ReposUpdateReleaseRequest
 * @version 1.1.4
 */
class ReposUpdateReleaseRequest {
    /**
     * Constructs a new <code>ReposUpdateReleaseRequest</code>.
     * @alias module:model/ReposUpdateReleaseRequest
     */
    constructor() { 
        
        ReposUpdateReleaseRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ReposUpdateReleaseRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReposUpdateReleaseRequest} obj Optional instance to populate.
     * @return {module:model/ReposUpdateReleaseRequest} The populated <code>ReposUpdateReleaseRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReposUpdateReleaseRequest();

            if (data.hasOwnProperty('tag_name')) {
                obj['tag_name'] = ApiClient.convertToType(data['tag_name'], 'String');
            }
            if (data.hasOwnProperty('target_commitish')) {
                obj['target_commitish'] = ApiClient.convertToType(data['target_commitish'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('draft')) {
                obj['draft'] = ApiClient.convertToType(data['draft'], 'Boolean');
            }
            if (data.hasOwnProperty('prerelease')) {
                obj['prerelease'] = ApiClient.convertToType(data['prerelease'], 'Boolean');
            }
            if (data.hasOwnProperty('make_latest')) {
                obj['make_latest'] = ApiClient.convertToType(data['make_latest'], 'String');
            }
            if (data.hasOwnProperty('discussion_category_name')) {
                obj['discussion_category_name'] = ApiClient.convertToType(data['discussion_category_name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReposUpdateReleaseRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReposUpdateReleaseRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['tag_name'] && !(typeof data['tag_name'] === 'string' || data['tag_name'] instanceof String)) {
            throw new Error("Expected the field `tag_name` to be a primitive type in the JSON string but got " + data['tag_name']);
        }
        // ensure the json data is a string
        if (data['target_commitish'] && !(typeof data['target_commitish'] === 'string' || data['target_commitish'] instanceof String)) {
            throw new Error("Expected the field `target_commitish` to be a primitive type in the JSON string but got " + data['target_commitish']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['make_latest'] && !(typeof data['make_latest'] === 'string' || data['make_latest'] instanceof String)) {
            throw new Error("Expected the field `make_latest` to be a primitive type in the JSON string but got " + data['make_latest']);
        }
        // ensure the json data is a string
        if (data['discussion_category_name'] && !(typeof data['discussion_category_name'] === 'string' || data['discussion_category_name'] instanceof String)) {
            throw new Error("Expected the field `discussion_category_name` to be a primitive type in the JSON string but got " + data['discussion_category_name']);
        }

        return true;
    }


}



/**
 * The name of the tag.
 * @member {String} tag_name
 */
ReposUpdateReleaseRequest.prototype['tag_name'] = undefined;

/**
 * Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch.
 * @member {String} target_commitish
 */
ReposUpdateReleaseRequest.prototype['target_commitish'] = undefined;

/**
 * The name of the release.
 * @member {String} name
 */
ReposUpdateReleaseRequest.prototype['name'] = undefined;

/**
 * Text describing the contents of the tag.
 * @member {String} body
 */
ReposUpdateReleaseRequest.prototype['body'] = undefined;

/**
 * `true` makes the release a draft, and `false` publishes the release.
 * @member {Boolean} draft
 */
ReposUpdateReleaseRequest.prototype['draft'] = undefined;

/**
 * `true` to identify the release as a prerelease, `false` to identify the release as a full release.
 * @member {Boolean} prerelease
 */
ReposUpdateReleaseRequest.prototype['prerelease'] = undefined;

/**
 * Specifies whether this release should be set as the latest release for the repository. Drafts and prereleases cannot be set as latest. Defaults to `true` for newly published releases. `legacy` specifies that the latest release should be determined based on the release creation date and higher semantic version.
 * @member {module:model/ReposUpdateReleaseRequest.MakeLatestEnum} make_latest
 * @default 'true'
 */
ReposUpdateReleaseRequest.prototype['make_latest'] = 'true';

/**
 * If specified, a discussion of the specified category is created and linked to the release. The value must be a category that already exists in the repository. If there is already a discussion linked to the release, this parameter is ignored. For more information, see \"[Managing categories for discussions in your repository](https://docs.github.com/discussions/managing-discussions-for-your-community/managing-categories-for-discussions-in-your-repository).\"
 * @member {String} discussion_category_name
 */
ReposUpdateReleaseRequest.prototype['discussion_category_name'] = undefined;





/**
 * Allowed values for the <code>make_latest</code> property.
 * @enum {String}
 * @readonly
 */
ReposUpdateReleaseRequest['MakeLatestEnum'] = {

    /**
     * value: "true"
     * @const
     */
    "true": "true",

    /**
     * value: "false"
     * @const
     */
    "false": "false",

    /**
     * value: "legacy"
     * @const
     */
    "legacy": "legacy"
};



export default ReposUpdateReleaseRequest;

