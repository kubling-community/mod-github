/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ActionsCreateOrgVariableRequest model module.
 * @module model/ActionsCreateOrgVariableRequest
 * @version 1.1.4
 */
class ActionsCreateOrgVariableRequest {
    /**
     * Constructs a new <code>ActionsCreateOrgVariableRequest</code>.
     * @alias module:model/ActionsCreateOrgVariableRequest
     * @param name {String} The name of the variable.
     * @param value {String} The value of the variable.
     * @param visibility {module:model/ActionsCreateOrgVariableRequest.VisibilityEnum} The type of repositories in the organization that can access the variable. `selected` means only the repositories specified by `selected_repository_ids` can access the variable.
     */
    constructor(name, value, visibility) { 
        
        ActionsCreateOrgVariableRequest.initialize(this, name, value, visibility);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, value, visibility) { 
        obj['name'] = name;
        obj['value'] = value;
        obj['visibility'] = visibility;
    }

    /**
     * Constructs a <code>ActionsCreateOrgVariableRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ActionsCreateOrgVariableRequest} obj Optional instance to populate.
     * @return {module:model/ActionsCreateOrgVariableRequest} The populated <code>ActionsCreateOrgVariableRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ActionsCreateOrgVariableRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
            if (data.hasOwnProperty('visibility')) {
                obj['visibility'] = ApiClient.convertToType(data['visibility'], 'String');
            }
            if (data.hasOwnProperty('selected_repository_ids')) {
                obj['selected_repository_ids'] = ApiClient.convertToType(data['selected_repository_ids'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ActionsCreateOrgVariableRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ActionsCreateOrgVariableRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ActionsCreateOrgVariableRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }
        // ensure the json data is a string
        if (data['visibility'] && !(typeof data['visibility'] === 'string' || data['visibility'] instanceof String)) {
            throw new Error("Expected the field `visibility` to be a primitive type in the JSON string but got " + data['visibility']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['selected_repository_ids'])) {
            throw new Error("Expected the field `selected_repository_ids` to be an array in the JSON data but got " + data['selected_repository_ids']);
        }

        return true;
    }


}

ActionsCreateOrgVariableRequest.RequiredProperties = ["name", "value", "visibility"];

/**
 * The name of the variable.
 * @member {String} name
 */
ActionsCreateOrgVariableRequest.prototype['name'] = undefined;

/**
 * The value of the variable.
 * @member {String} value
 */
ActionsCreateOrgVariableRequest.prototype['value'] = undefined;

/**
 * The type of repositories in the organization that can access the variable. `selected` means only the repositories specified by `selected_repository_ids` can access the variable.
 * @member {module:model/ActionsCreateOrgVariableRequest.VisibilityEnum} visibility
 */
ActionsCreateOrgVariableRequest.prototype['visibility'] = undefined;

/**
 * An array of repository ids that can access the organization variable. You can only provide a list of repository ids when the `visibility` is set to `selected`.
 * @member {Array.<Number>} selected_repository_ids
 */
ActionsCreateOrgVariableRequest.prototype['selected_repository_ids'] = undefined;





/**
 * Allowed values for the <code>visibility</code> property.
 * @enum {String}
 * @readonly
 */
ActionsCreateOrgVariableRequest['VisibilityEnum'] = {

    /**
     * value: "all"
     * @const
     */
    "all": "all",

    /**
     * value: "private"
     * @const
     */
    "private": "private",

    /**
     * value: "selected"
     * @const
     */
    "selected": "selected"
};



export default ActionsCreateOrgVariableRequest;

