/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApiOverviewDomains from './ApiOverviewDomains';
import ApiOverviewSshKeyFingerprints from './ApiOverviewSshKeyFingerprints';

/**
 * The ApiOverview model module.
 * @module model/ApiOverview
 * @version 1.1.4
 */
class ApiOverview {
    /**
     * Constructs a new <code>ApiOverview</code>.
     * Api Overview
     * @alias module:model/ApiOverview
     * @param verifiablePasswordAuthentication {Boolean} 
     */
    constructor(verifiablePasswordAuthentication) { 
        
        ApiOverview.initialize(this, verifiablePasswordAuthentication);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, verifiablePasswordAuthentication) { 
        obj['verifiable_password_authentication'] = verifiablePasswordAuthentication;
    }

    /**
     * Constructs a <code>ApiOverview</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiOverview} obj Optional instance to populate.
     * @return {module:model/ApiOverview} The populated <code>ApiOverview</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiOverview();

            if (data.hasOwnProperty('verifiable_password_authentication')) {
                obj['verifiable_password_authentication'] = ApiClient.convertToType(data['verifiable_password_authentication'], 'Boolean');
            }
            if (data.hasOwnProperty('ssh_key_fingerprints')) {
                obj['ssh_key_fingerprints'] = ApiOverviewSshKeyFingerprints.constructFromObject(data['ssh_key_fingerprints']);
            }
            if (data.hasOwnProperty('ssh_keys')) {
                obj['ssh_keys'] = ApiClient.convertToType(data['ssh_keys'], ['String']);
            }
            if (data.hasOwnProperty('hooks')) {
                obj['hooks'] = ApiClient.convertToType(data['hooks'], ['String']);
            }
            if (data.hasOwnProperty('github_enterprise_importer')) {
                obj['github_enterprise_importer'] = ApiClient.convertToType(data['github_enterprise_importer'], ['String']);
            }
            if (data.hasOwnProperty('web')) {
                obj['web'] = ApiClient.convertToType(data['web'], ['String']);
            }
            if (data.hasOwnProperty('api')) {
                obj['api'] = ApiClient.convertToType(data['api'], ['String']);
            }
            if (data.hasOwnProperty('git')) {
                obj['git'] = ApiClient.convertToType(data['git'], ['String']);
            }
            if (data.hasOwnProperty('packages')) {
                obj['packages'] = ApiClient.convertToType(data['packages'], ['String']);
            }
            if (data.hasOwnProperty('pages')) {
                obj['pages'] = ApiClient.convertToType(data['pages'], ['String']);
            }
            if (data.hasOwnProperty('importer')) {
                obj['importer'] = ApiClient.convertToType(data['importer'], ['String']);
            }
            if (data.hasOwnProperty('actions')) {
                obj['actions'] = ApiClient.convertToType(data['actions'], ['String']);
            }
            if (data.hasOwnProperty('actions_macos')) {
                obj['actions_macos'] = ApiClient.convertToType(data['actions_macos'], ['String']);
            }
            if (data.hasOwnProperty('dependabot')) {
                obj['dependabot'] = ApiClient.convertToType(data['dependabot'], ['String']);
            }
            if (data.hasOwnProperty('domains')) {
                obj['domains'] = ApiOverviewDomains.constructFromObject(data['domains']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiOverview</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiOverview</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiOverview.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `ssh_key_fingerprints`
        if (data['ssh_key_fingerprints']) { // data not null
          ApiOverviewSshKeyFingerprints.validateJSON(data['ssh_key_fingerprints']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['ssh_keys'])) {
            throw new Error("Expected the field `ssh_keys` to be an array in the JSON data but got " + data['ssh_keys']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['hooks'])) {
            throw new Error("Expected the field `hooks` to be an array in the JSON data but got " + data['hooks']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['github_enterprise_importer'])) {
            throw new Error("Expected the field `github_enterprise_importer` to be an array in the JSON data but got " + data['github_enterprise_importer']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['web'])) {
            throw new Error("Expected the field `web` to be an array in the JSON data but got " + data['web']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['api'])) {
            throw new Error("Expected the field `api` to be an array in the JSON data but got " + data['api']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['git'])) {
            throw new Error("Expected the field `git` to be an array in the JSON data but got " + data['git']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['packages'])) {
            throw new Error("Expected the field `packages` to be an array in the JSON data but got " + data['packages']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['pages'])) {
            throw new Error("Expected the field `pages` to be an array in the JSON data but got " + data['pages']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['importer'])) {
            throw new Error("Expected the field `importer` to be an array in the JSON data but got " + data['importer']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['actions'])) {
            throw new Error("Expected the field `actions` to be an array in the JSON data but got " + data['actions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['actions_macos'])) {
            throw new Error("Expected the field `actions_macos` to be an array in the JSON data but got " + data['actions_macos']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['dependabot'])) {
            throw new Error("Expected the field `dependabot` to be an array in the JSON data but got " + data['dependabot']);
        }
        // validate the optional field `domains`
        if (data['domains']) { // data not null
          ApiOverviewDomains.validateJSON(data['domains']);
        }

        return true;
    }


}

ApiOverview.RequiredProperties = ["verifiable_password_authentication"];

/**
 * @member {Boolean} verifiable_password_authentication
 */
ApiOverview.prototype['verifiable_password_authentication'] = undefined;

/**
 * @member {module:model/ApiOverviewSshKeyFingerprints} ssh_key_fingerprints
 */
ApiOverview.prototype['ssh_key_fingerprints'] = undefined;

/**
 * @member {Array.<String>} ssh_keys
 */
ApiOverview.prototype['ssh_keys'] = undefined;

/**
 * @member {Array.<String>} hooks
 */
ApiOverview.prototype['hooks'] = undefined;

/**
 * @member {Array.<String>} github_enterprise_importer
 */
ApiOverview.prototype['github_enterprise_importer'] = undefined;

/**
 * @member {Array.<String>} web
 */
ApiOverview.prototype['web'] = undefined;

/**
 * @member {Array.<String>} api
 */
ApiOverview.prototype['api'] = undefined;

/**
 * @member {Array.<String>} git
 */
ApiOverview.prototype['git'] = undefined;

/**
 * @member {Array.<String>} packages
 */
ApiOverview.prototype['packages'] = undefined;

/**
 * @member {Array.<String>} pages
 */
ApiOverview.prototype['pages'] = undefined;

/**
 * @member {Array.<String>} importer
 */
ApiOverview.prototype['importer'] = undefined;

/**
 * @member {Array.<String>} actions
 */
ApiOverview.prototype['actions'] = undefined;

/**
 * @member {Array.<String>} actions_macos
 */
ApiOverview.prototype['actions_macos'] = undefined;

/**
 * @member {Array.<String>} dependabot
 */
ApiOverview.prototype['dependabot'] = undefined;

/**
 * @member {module:model/ApiOverviewDomains} domains
 */
ApiOverview.prototype['domains'] = undefined;






export default ApiOverview;

