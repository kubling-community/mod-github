/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NullableSimpleUser from './NullableSimpleUser';

/**
 * The OrganizationRole model module.
 * @module model/OrganizationRole
 * @version 1.1.4
 */
class OrganizationRole {
    /**
     * Constructs a new <code>OrganizationRole</code>.
     * Organization roles
     * @alias module:model/OrganizationRole
     * @param id {Number} The unique identifier of the role.
     * @param name {String} The name of the role.
     * @param permissions {Array.<String>} A list of permissions included in this role.
     * @param organization {module:model/NullableSimpleUser} 
     * @param createdAt {Date} The date and time the role was created.
     * @param updatedAt {Date} The date and time the role was last updated.
     */
    constructor(id, name, permissions, organization, createdAt, updatedAt) { 
        
        OrganizationRole.initialize(this, id, name, permissions, organization, createdAt, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, permissions, organization, createdAt, updatedAt) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['permissions'] = permissions;
        obj['organization'] = organization;
        obj['created_at'] = createdAt;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>OrganizationRole</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationRole} obj Optional instance to populate.
     * @return {module:model/OrganizationRole} The populated <code>OrganizationRole</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationRole();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], ['String']);
            }
            if (data.hasOwnProperty('organization')) {
                obj['organization'] = NullableSimpleUser.constructFromObject(data['organization']);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationRole</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationRole</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrganizationRole.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['permissions'])) {
            throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
        }
        // validate the optional field `organization`
        if (data['organization']) { // data not null
          NullableSimpleUser.validateJSON(data['organization']);
        }

        return true;
    }


}

OrganizationRole.RequiredProperties = ["id", "name", "permissions", "organization", "created_at", "updated_at"];

/**
 * The unique identifier of the role.
 * @member {Number} id
 */
OrganizationRole.prototype['id'] = undefined;

/**
 * The name of the role.
 * @member {String} name
 */
OrganizationRole.prototype['name'] = undefined;

/**
 * A short description about who this role is for or what permissions it grants.
 * @member {String} description
 */
OrganizationRole.prototype['description'] = undefined;

/**
 * A list of permissions included in this role.
 * @member {Array.<String>} permissions
 */
OrganizationRole.prototype['permissions'] = undefined;

/**
 * @member {module:model/NullableSimpleUser} organization
 */
OrganizationRole.prototype['organization'] = undefined;

/**
 * The date and time the role was created.
 * @member {Date} created_at
 */
OrganizationRole.prototype['created_at'] = undefined;

/**
 * The date and time the role was last updated.
 * @member {Date} updated_at
 */
OrganizationRole.prototype['updated_at'] = undefined;






export default OrganizationRole;

