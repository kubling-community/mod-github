/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GlobalAdvisoryIdentifiersInner model module.
 * @module model/GlobalAdvisoryIdentifiersInner
 * @version 1.1.4
 */
class GlobalAdvisoryIdentifiersInner {
    /**
     * Constructs a new <code>GlobalAdvisoryIdentifiersInner</code>.
     * @alias module:model/GlobalAdvisoryIdentifiersInner
     * @param type {module:model/GlobalAdvisoryIdentifiersInner.TypeEnum} The type of identifier.
     * @param value {String} The identifier value.
     */
    constructor(type, value) { 
        
        GlobalAdvisoryIdentifiersInner.initialize(this, type, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type, value) { 
        obj['type'] = type;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>GlobalAdvisoryIdentifiersInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GlobalAdvisoryIdentifiersInner} obj Optional instance to populate.
     * @return {module:model/GlobalAdvisoryIdentifiersInner} The populated <code>GlobalAdvisoryIdentifiersInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GlobalAdvisoryIdentifiersInner();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GlobalAdvisoryIdentifiersInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GlobalAdvisoryIdentifiersInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GlobalAdvisoryIdentifiersInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

GlobalAdvisoryIdentifiersInner.RequiredProperties = ["type", "value"];

/**
 * The type of identifier.
 * @member {module:model/GlobalAdvisoryIdentifiersInner.TypeEnum} type
 */
GlobalAdvisoryIdentifiersInner.prototype['type'] = undefined;

/**
 * The identifier value.
 * @member {String} value
 */
GlobalAdvisoryIdentifiersInner.prototype['value'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
GlobalAdvisoryIdentifiersInner['TypeEnum'] = {

    /**
     * value: "CVE"
     * @const
     */
    "CVE": "CVE",

    /**
     * value: "GHSA"
     * @const
     */
    "GHSA": "GHSA"
};



export default GlobalAdvisoryIdentifiersInner;

