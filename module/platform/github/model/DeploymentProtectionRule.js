/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomDeploymentRuleApp from './CustomDeploymentRuleApp';

/**
 * The DeploymentProtectionRule model module.
 * @module model/DeploymentProtectionRule
 * @version 1.1.4
 */
class DeploymentProtectionRule {
    /**
     * Constructs a new <code>DeploymentProtectionRule</code>.
     * Deployment protection rule
     * @alias module:model/DeploymentProtectionRule
     * @param id {Number} The unique identifier for the deployment protection rule.
     * @param nodeId {String} The node ID for the deployment protection rule.
     * @param enabled {Boolean} Whether the deployment protection rule is enabled for the environment.
     * @param app {module:model/CustomDeploymentRuleApp} 
     */
    constructor(id, nodeId, enabled, app) { 
        
        DeploymentProtectionRule.initialize(this, id, nodeId, enabled, app);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, nodeId, enabled, app) { 
        obj['id'] = id;
        obj['node_id'] = nodeId;
        obj['enabled'] = enabled;
        obj['app'] = app;
    }

    /**
     * Constructs a <code>DeploymentProtectionRule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeploymentProtectionRule} obj Optional instance to populate.
     * @return {module:model/DeploymentProtectionRule} The populated <code>DeploymentProtectionRule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeploymentProtectionRule();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('node_id')) {
                obj['node_id'] = ApiClient.convertToType(data['node_id'], 'String');
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('app')) {
                obj['app'] = CustomDeploymentRuleApp.constructFromObject(data['app']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeploymentProtectionRule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeploymentProtectionRule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeploymentProtectionRule.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['node_id'] && !(typeof data['node_id'] === 'string' || data['node_id'] instanceof String)) {
            throw new Error("Expected the field `node_id` to be a primitive type in the JSON string but got " + data['node_id']);
        }
        // validate the optional field `app`
        if (data['app']) { // data not null
          CustomDeploymentRuleApp.validateJSON(data['app']);
        }

        return true;
    }


}

DeploymentProtectionRule.RequiredProperties = ["id", "node_id", "enabled", "app"];

/**
 * The unique identifier for the deployment protection rule.
 * @member {Number} id
 */
DeploymentProtectionRule.prototype['id'] = undefined;

/**
 * The node ID for the deployment protection rule.
 * @member {String} node_id
 */
DeploymentProtectionRule.prototype['node_id'] = undefined;

/**
 * Whether the deployment protection rule is enabled for the environment.
 * @member {Boolean} enabled
 */
DeploymentProtectionRule.prototype['enabled'] = undefined;

/**
 * @member {module:model/CustomDeploymentRuleApp} app
 */
DeploymentProtectionRule.prototype['app'] = undefined;






export default DeploymentProtectionRule;

