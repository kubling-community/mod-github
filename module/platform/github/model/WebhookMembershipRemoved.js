/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EnterpriseWebhooks from './EnterpriseWebhooks';
import OrganizationSimpleWebhooks from './OrganizationSimpleWebhooks';
import RepositoryWebhooks from './RepositoryWebhooks';
import SimpleInstallation from './SimpleInstallation';
import User from './User';
import WebhooksTeam from './WebhooksTeam';
import WebhooksUser from './WebhooksUser';

/**
 * The WebhookMembershipRemoved model module.
 * @module model/WebhookMembershipRemoved
 * @version 1.1.4
 */
class WebhookMembershipRemoved {
    /**
     * Constructs a new <code>WebhookMembershipRemoved</code>.
     * @alias module:model/WebhookMembershipRemoved
     * @param action {module:model/WebhookMembershipRemoved.ActionEnum} 
     * @param member {module:model/WebhooksUser} 
     * @param organization {module:model/OrganizationSimpleWebhooks} 
     * @param scope {module:model/WebhookMembershipRemoved.ScopeEnum} The scope of the membership. Currently, can only be `team`.
     * @param sender {module:model/User} 
     * @param team {module:model/WebhooksTeam} 
     */
    constructor(action, member, organization, scope, sender, team) { 
        
        WebhookMembershipRemoved.initialize(this, action, member, organization, scope, sender, team);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action, member, organization, scope, sender, team) { 
        obj['action'] = action;
        obj['member'] = member;
        obj['organization'] = organization;
        obj['scope'] = scope;
        obj['sender'] = sender;
        obj['team'] = team;
    }

    /**
     * Constructs a <code>WebhookMembershipRemoved</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WebhookMembershipRemoved} obj Optional instance to populate.
     * @return {module:model/WebhookMembershipRemoved} The populated <code>WebhookMembershipRemoved</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WebhookMembershipRemoved();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('enterprise')) {
                obj['enterprise'] = EnterpriseWebhooks.constructFromObject(data['enterprise']);
            }
            if (data.hasOwnProperty('installation')) {
                obj['installation'] = SimpleInstallation.constructFromObject(data['installation']);
            }
            if (data.hasOwnProperty('member')) {
                obj['member'] = WebhooksUser.constructFromObject(data['member']);
            }
            if (data.hasOwnProperty('organization')) {
                obj['organization'] = OrganizationSimpleWebhooks.constructFromObject(data['organization']);
            }
            if (data.hasOwnProperty('repository')) {
                obj['repository'] = RepositoryWebhooks.constructFromObject(data['repository']);
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
            if (data.hasOwnProperty('sender')) {
                obj['sender'] = User.constructFromObject(data['sender']);
            }
            if (data.hasOwnProperty('team')) {
                obj['team'] = WebhooksTeam.constructFromObject(data['team']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WebhookMembershipRemoved</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WebhookMembershipRemoved</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WebhookMembershipRemoved.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // validate the optional field `enterprise`
        if (data['enterprise']) { // data not null
          EnterpriseWebhooks.validateJSON(data['enterprise']);
        }
        // validate the optional field `installation`
        if (data['installation']) { // data not null
          SimpleInstallation.validateJSON(data['installation']);
        }
        // validate the optional field `member`
        if (data['member']) { // data not null
          WebhooksUser.validateJSON(data['member']);
        }
        // validate the optional field `organization`
        if (data['organization']) { // data not null
          OrganizationSimpleWebhooks.validateJSON(data['organization']);
        }
        // validate the optional field `repository`
        if (data['repository']) { // data not null
          RepositoryWebhooks.validateJSON(data['repository']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }
        // validate the optional field `sender`
        if (data['sender']) { // data not null
          User.validateJSON(data['sender']);
        }
        // validate the optional field `team`
        if (data['team']) { // data not null
          WebhooksTeam.validateJSON(data['team']);
        }

        return true;
    }


}

WebhookMembershipRemoved.RequiredProperties = ["action", "member", "organization", "scope", "sender", "team"];

/**
 * @member {module:model/WebhookMembershipRemoved.ActionEnum} action
 */
WebhookMembershipRemoved.prototype['action'] = undefined;

/**
 * @member {module:model/EnterpriseWebhooks} enterprise
 */
WebhookMembershipRemoved.prototype['enterprise'] = undefined;

/**
 * @member {module:model/SimpleInstallation} installation
 */
WebhookMembershipRemoved.prototype['installation'] = undefined;

/**
 * @member {module:model/WebhooksUser} member
 */
WebhookMembershipRemoved.prototype['member'] = undefined;

/**
 * @member {module:model/OrganizationSimpleWebhooks} organization
 */
WebhookMembershipRemoved.prototype['organization'] = undefined;

/**
 * @member {module:model/RepositoryWebhooks} repository
 */
WebhookMembershipRemoved.prototype['repository'] = undefined;

/**
 * The scope of the membership. Currently, can only be `team`.
 * @member {module:model/WebhookMembershipRemoved.ScopeEnum} scope
 */
WebhookMembershipRemoved.prototype['scope'] = undefined;

/**
 * @member {module:model/User} sender
 */
WebhookMembershipRemoved.prototype['sender'] = undefined;

/**
 * @member {module:model/WebhooksTeam} team
 */
WebhookMembershipRemoved.prototype['team'] = undefined;





/**
 * Allowed values for the <code>action</code> property.
 * @enum {String}
 * @readonly
 */
WebhookMembershipRemoved['ActionEnum'] = {

    /**
     * value: "removed"
     * @const
     */
    "removed": "removed"
};


/**
 * Allowed values for the <code>scope</code> property.
 * @enum {String}
 * @readonly
 */
WebhookMembershipRemoved['ScopeEnum'] = {

    /**
     * value: "team"
     * @const
     */
    "team": "team",

    /**
     * value: "organization"
     * @const
     */
    "organization": "organization"
};



export default WebhookMembershipRemoved;

