/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SshSigningKey model module.
 * @module model/SshSigningKey
 * @version 1.1.4
 */
class SshSigningKey {
    /**
     * Constructs a new <code>SshSigningKey</code>.
     * A public SSH key used to sign Git commits
     * @alias module:model/SshSigningKey
     * @param key {String} 
     * @param id {Number} 
     * @param title {String} 
     * @param createdAt {Date} 
     */
    constructor(key, id, title, createdAt) { 
        
        SshSigningKey.initialize(this, key, id, title, createdAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, key, id, title, createdAt) { 
        obj['key'] = key;
        obj['id'] = id;
        obj['title'] = title;
        obj['created_at'] = createdAt;
    }

    /**
     * Constructs a <code>SshSigningKey</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SshSigningKey} obj Optional instance to populate.
     * @return {module:model/SshSigningKey} The populated <code>SshSigningKey</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SshSigningKey();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SshSigningKey</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SshSigningKey</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SshSigningKey.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }

        return true;
    }


}

SshSigningKey.RequiredProperties = ["key", "id", "title", "created_at"];

/**
 * @member {String} key
 */
SshSigningKey.prototype['key'] = undefined;

/**
 * @member {Number} id
 */
SshSigningKey.prototype['id'] = undefined;

/**
 * @member {String} title
 */
SshSigningKey.prototype['title'] = undefined;

/**
 * @member {Date} created_at
 */
SshSigningKey.prototype['created_at'] = undefined;






export default SshSigningKey;

