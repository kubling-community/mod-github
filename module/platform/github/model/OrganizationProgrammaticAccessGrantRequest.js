/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OrganizationProgrammaticAccessGrantRequestPermissions from './OrganizationProgrammaticAccessGrantRequestPermissions';
import SimpleUser from './SimpleUser';

/**
 * The OrganizationProgrammaticAccessGrantRequest model module.
 * @module model/OrganizationProgrammaticAccessGrantRequest
 * @version 1.1.4
 */
class OrganizationProgrammaticAccessGrantRequest {
    /**
     * Constructs a new <code>OrganizationProgrammaticAccessGrantRequest</code>.
     * Minimal representation of an organization programmatic access grant request for enumerations
     * @alias module:model/OrganizationProgrammaticAccessGrantRequest
     * @param id {Number} Unique identifier of the request for access via fine-grained personal access token. The `pat_request_id` used to review PAT requests.
     * @param reason {String} Reason for requesting access.
     * @param owner {module:model/SimpleUser} 
     * @param repositorySelection {module:model/OrganizationProgrammaticAccessGrantRequest.RepositorySelectionEnum} Type of repository selection requested.
     * @param repositoriesUrl {String} URL to the list of repositories requested to be accessed via fine-grained personal access token. Should only be followed when `repository_selection` is `subset`.
     * @param permissions {module:model/OrganizationProgrammaticAccessGrantRequestPermissions} 
     * @param createdAt {String} Date and time when the request for access was created.
     * @param tokenExpired {Boolean} Whether the associated fine-grained personal access token has expired.
     * @param tokenExpiresAt {String} Date and time when the associated fine-grained personal access token expires.
     * @param tokenLastUsedAt {String} Date and time when the associated fine-grained personal access token was last used for authentication.
     */
    constructor(id, reason, owner, repositorySelection, repositoriesUrl, permissions, createdAt, tokenExpired, tokenExpiresAt, tokenLastUsedAt) { 
        
        OrganizationProgrammaticAccessGrantRequest.initialize(this, id, reason, owner, repositorySelection, repositoriesUrl, permissions, createdAt, tokenExpired, tokenExpiresAt, tokenLastUsedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, reason, owner, repositorySelection, repositoriesUrl, permissions, createdAt, tokenExpired, tokenExpiresAt, tokenLastUsedAt) { 
        obj['id'] = id;
        obj['reason'] = reason;
        obj['owner'] = owner;
        obj['repository_selection'] = repositorySelection;
        obj['repositories_url'] = repositoriesUrl;
        obj['permissions'] = permissions;
        obj['created_at'] = createdAt;
        obj['token_expired'] = tokenExpired;
        obj['token_expires_at'] = tokenExpiresAt;
        obj['token_last_used_at'] = tokenLastUsedAt;
    }

    /**
     * Constructs a <code>OrganizationProgrammaticAccessGrantRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrganizationProgrammaticAccessGrantRequest} obj Optional instance to populate.
     * @return {module:model/OrganizationProgrammaticAccessGrantRequest} The populated <code>OrganizationProgrammaticAccessGrantRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrganizationProgrammaticAccessGrantRequest();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('owner')) {
                obj['owner'] = SimpleUser.constructFromObject(data['owner']);
            }
            if (data.hasOwnProperty('repository_selection')) {
                obj['repository_selection'] = ApiClient.convertToType(data['repository_selection'], 'String');
            }
            if (data.hasOwnProperty('repositories_url')) {
                obj['repositories_url'] = ApiClient.convertToType(data['repositories_url'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = OrganizationProgrammaticAccessGrantRequestPermissions.constructFromObject(data['permissions']);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'String');
            }
            if (data.hasOwnProperty('token_expired')) {
                obj['token_expired'] = ApiClient.convertToType(data['token_expired'], 'Boolean');
            }
            if (data.hasOwnProperty('token_expires_at')) {
                obj['token_expires_at'] = ApiClient.convertToType(data['token_expires_at'], 'String');
            }
            if (data.hasOwnProperty('token_last_used_at')) {
                obj['token_last_used_at'] = ApiClient.convertToType(data['token_last_used_at'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrganizationProgrammaticAccessGrantRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrganizationProgrammaticAccessGrantRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrganizationProgrammaticAccessGrantRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // validate the optional field `owner`
        if (data['owner']) { // data not null
          SimpleUser.validateJSON(data['owner']);
        }
        // ensure the json data is a string
        if (data['repository_selection'] && !(typeof data['repository_selection'] === 'string' || data['repository_selection'] instanceof String)) {
            throw new Error("Expected the field `repository_selection` to be a primitive type in the JSON string but got " + data['repository_selection']);
        }
        // ensure the json data is a string
        if (data['repositories_url'] && !(typeof data['repositories_url'] === 'string' || data['repositories_url'] instanceof String)) {
            throw new Error("Expected the field `repositories_url` to be a primitive type in the JSON string but got " + data['repositories_url']);
        }
        // validate the optional field `permissions`
        if (data['permissions']) { // data not null
          OrganizationProgrammaticAccessGrantRequestPermissions.validateJSON(data['permissions']);
        }
        // ensure the json data is a string
        if (data['created_at'] && !(typeof data['created_at'] === 'string' || data['created_at'] instanceof String)) {
            throw new Error("Expected the field `created_at` to be a primitive type in the JSON string but got " + data['created_at']);
        }
        // ensure the json data is a string
        if (data['token_expires_at'] && !(typeof data['token_expires_at'] === 'string' || data['token_expires_at'] instanceof String)) {
            throw new Error("Expected the field `token_expires_at` to be a primitive type in the JSON string but got " + data['token_expires_at']);
        }
        // ensure the json data is a string
        if (data['token_last_used_at'] && !(typeof data['token_last_used_at'] === 'string' || data['token_last_used_at'] instanceof String)) {
            throw new Error("Expected the field `token_last_used_at` to be a primitive type in the JSON string but got " + data['token_last_used_at']);
        }

        return true;
    }


}

OrganizationProgrammaticAccessGrantRequest.RequiredProperties = ["id", "reason", "owner", "repository_selection", "repositories_url", "permissions", "created_at", "token_expired", "token_expires_at", "token_last_used_at"];

/**
 * Unique identifier of the request for access via fine-grained personal access token. The `pat_request_id` used to review PAT requests.
 * @member {Number} id
 */
OrganizationProgrammaticAccessGrantRequest.prototype['id'] = undefined;

/**
 * Reason for requesting access.
 * @member {String} reason
 */
OrganizationProgrammaticAccessGrantRequest.prototype['reason'] = undefined;

/**
 * @member {module:model/SimpleUser} owner
 */
OrganizationProgrammaticAccessGrantRequest.prototype['owner'] = undefined;

/**
 * Type of repository selection requested.
 * @member {module:model/OrganizationProgrammaticAccessGrantRequest.RepositorySelectionEnum} repository_selection
 */
OrganizationProgrammaticAccessGrantRequest.prototype['repository_selection'] = undefined;

/**
 * URL to the list of repositories requested to be accessed via fine-grained personal access token. Should only be followed when `repository_selection` is `subset`.
 * @member {String} repositories_url
 */
OrganizationProgrammaticAccessGrantRequest.prototype['repositories_url'] = undefined;

/**
 * @member {module:model/OrganizationProgrammaticAccessGrantRequestPermissions} permissions
 */
OrganizationProgrammaticAccessGrantRequest.prototype['permissions'] = undefined;

/**
 * Date and time when the request for access was created.
 * @member {String} created_at
 */
OrganizationProgrammaticAccessGrantRequest.prototype['created_at'] = undefined;

/**
 * Whether the associated fine-grained personal access token has expired.
 * @member {Boolean} token_expired
 */
OrganizationProgrammaticAccessGrantRequest.prototype['token_expired'] = undefined;

/**
 * Date and time when the associated fine-grained personal access token expires.
 * @member {String} token_expires_at
 */
OrganizationProgrammaticAccessGrantRequest.prototype['token_expires_at'] = undefined;

/**
 * Date and time when the associated fine-grained personal access token was last used for authentication.
 * @member {String} token_last_used_at
 */
OrganizationProgrammaticAccessGrantRequest.prototype['token_last_used_at'] = undefined;





/**
 * Allowed values for the <code>repository_selection</code> property.
 * @enum {String}
 * @readonly
 */
OrganizationProgrammaticAccessGrantRequest['RepositorySelectionEnum'] = {

    /**
     * value: "none"
     * @const
     */
    "none": "none",

    /**
     * value: "all"
     * @const
     */
    "all": "all",

    /**
     * value: "subset"
     * @const
     */
    "subset": "subset"
};



export default OrganizationProgrammaticAccessGrantRequest;

