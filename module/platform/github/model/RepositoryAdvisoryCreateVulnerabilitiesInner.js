/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RepositoryAdvisoryCreateVulnerabilitiesInnerPackage from './RepositoryAdvisoryCreateVulnerabilitiesInnerPackage';

/**
 * The RepositoryAdvisoryCreateVulnerabilitiesInner model module.
 * @module model/RepositoryAdvisoryCreateVulnerabilitiesInner
 * @version 1.1.4
 */
class RepositoryAdvisoryCreateVulnerabilitiesInner {
    /**
     * Constructs a new <code>RepositoryAdvisoryCreateVulnerabilitiesInner</code>.
     * @alias module:model/RepositoryAdvisoryCreateVulnerabilitiesInner
     * @param _package {module:model/RepositoryAdvisoryCreateVulnerabilitiesInnerPackage} 
     */
    constructor(_package) { 
        
        RepositoryAdvisoryCreateVulnerabilitiesInner.initialize(this, _package);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, _package) { 
        obj['package'] = _package;
    }

    /**
     * Constructs a <code>RepositoryAdvisoryCreateVulnerabilitiesInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RepositoryAdvisoryCreateVulnerabilitiesInner} obj Optional instance to populate.
     * @return {module:model/RepositoryAdvisoryCreateVulnerabilitiesInner} The populated <code>RepositoryAdvisoryCreateVulnerabilitiesInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RepositoryAdvisoryCreateVulnerabilitiesInner();

            if (data.hasOwnProperty('package')) {
                obj['package'] = RepositoryAdvisoryCreateVulnerabilitiesInnerPackage.constructFromObject(data['package']);
            }
            if (data.hasOwnProperty('vulnerable_version_range')) {
                obj['vulnerable_version_range'] = ApiClient.convertToType(data['vulnerable_version_range'], 'String');
            }
            if (data.hasOwnProperty('patched_versions')) {
                obj['patched_versions'] = ApiClient.convertToType(data['patched_versions'], 'String');
            }
            if (data.hasOwnProperty('vulnerable_functions')) {
                obj['vulnerable_functions'] = ApiClient.convertToType(data['vulnerable_functions'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RepositoryAdvisoryCreateVulnerabilitiesInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RepositoryAdvisoryCreateVulnerabilitiesInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RepositoryAdvisoryCreateVulnerabilitiesInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `package`
        if (data['package']) { // data not null
          RepositoryAdvisoryCreateVulnerabilitiesInnerPackage.validateJSON(data['package']);
        }
        // ensure the json data is a string
        if (data['vulnerable_version_range'] && !(typeof data['vulnerable_version_range'] === 'string' || data['vulnerable_version_range'] instanceof String)) {
            throw new Error("Expected the field `vulnerable_version_range` to be a primitive type in the JSON string but got " + data['vulnerable_version_range']);
        }
        // ensure the json data is a string
        if (data['patched_versions'] && !(typeof data['patched_versions'] === 'string' || data['patched_versions'] instanceof String)) {
            throw new Error("Expected the field `patched_versions` to be a primitive type in the JSON string but got " + data['patched_versions']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['vulnerable_functions'])) {
            throw new Error("Expected the field `vulnerable_functions` to be an array in the JSON data but got " + data['vulnerable_functions']);
        }

        return true;
    }


}

RepositoryAdvisoryCreateVulnerabilitiesInner.RequiredProperties = ["package"];

/**
 * @member {module:model/RepositoryAdvisoryCreateVulnerabilitiesInnerPackage} package
 */
RepositoryAdvisoryCreateVulnerabilitiesInner.prototype['package'] = undefined;

/**
 * The range of the package versions affected by the vulnerability.
 * @member {String} vulnerable_version_range
 */
RepositoryAdvisoryCreateVulnerabilitiesInner.prototype['vulnerable_version_range'] = undefined;

/**
 * The package version(s) that resolve the vulnerability.
 * @member {String} patched_versions
 */
RepositoryAdvisoryCreateVulnerabilitiesInner.prototype['patched_versions'] = undefined;

/**
 * The functions in the package that are affected.
 * @member {Array.<String>} vulnerable_functions
 */
RepositoryAdvisoryCreateVulnerabilitiesInner.prototype['vulnerable_functions'] = undefined;






export default RepositoryAdvisoryCreateVulnerabilitiesInner;

