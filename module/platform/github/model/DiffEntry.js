/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DiffEntry model module.
 * @module model/DiffEntry
 * @version 1.1.4
 */
class DiffEntry {
    /**
     * Constructs a new <code>DiffEntry</code>.
     * Diff Entry
     * @alias module:model/DiffEntry
     * @param sha {String} 
     * @param filename {String} 
     * @param status {module:model/DiffEntry.StatusEnum} 
     * @param additions {Number} 
     * @param deletions {Number} 
     * @param changes {Number} 
     * @param blobUrl {String} 
     * @param rawUrl {String} 
     * @param contentsUrl {String} 
     */
    constructor(sha, filename, status, additions, deletions, changes, blobUrl, rawUrl, contentsUrl) { 
        
        DiffEntry.initialize(this, sha, filename, status, additions, deletions, changes, blobUrl, rawUrl, contentsUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, sha, filename, status, additions, deletions, changes, blobUrl, rawUrl, contentsUrl) { 
        obj['sha'] = sha;
        obj['filename'] = filename;
        obj['status'] = status;
        obj['additions'] = additions;
        obj['deletions'] = deletions;
        obj['changes'] = changes;
        obj['blob_url'] = blobUrl;
        obj['raw_url'] = rawUrl;
        obj['contents_url'] = contentsUrl;
    }

    /**
     * Constructs a <code>DiffEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DiffEntry} obj Optional instance to populate.
     * @return {module:model/DiffEntry} The populated <code>DiffEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DiffEntry();

            if (data.hasOwnProperty('sha')) {
                obj['sha'] = ApiClient.convertToType(data['sha'], 'String');
            }
            if (data.hasOwnProperty('filename')) {
                obj['filename'] = ApiClient.convertToType(data['filename'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('additions')) {
                obj['additions'] = ApiClient.convertToType(data['additions'], 'Number');
            }
            if (data.hasOwnProperty('deletions')) {
                obj['deletions'] = ApiClient.convertToType(data['deletions'], 'Number');
            }
            if (data.hasOwnProperty('changes')) {
                obj['changes'] = ApiClient.convertToType(data['changes'], 'Number');
            }
            if (data.hasOwnProperty('blob_url')) {
                obj['blob_url'] = ApiClient.convertToType(data['blob_url'], 'String');
            }
            if (data.hasOwnProperty('raw_url')) {
                obj['raw_url'] = ApiClient.convertToType(data['raw_url'], 'String');
            }
            if (data.hasOwnProperty('contents_url')) {
                obj['contents_url'] = ApiClient.convertToType(data['contents_url'], 'String');
            }
            if (data.hasOwnProperty('patch')) {
                obj['patch'] = ApiClient.convertToType(data['patch'], 'String');
            }
            if (data.hasOwnProperty('previous_filename')) {
                obj['previous_filename'] = ApiClient.convertToType(data['previous_filename'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DiffEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DiffEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DiffEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['sha'] && !(typeof data['sha'] === 'string' || data['sha'] instanceof String)) {
            throw new Error("Expected the field `sha` to be a primitive type in the JSON string but got " + data['sha']);
        }
        // ensure the json data is a string
        if (data['filename'] && !(typeof data['filename'] === 'string' || data['filename'] instanceof String)) {
            throw new Error("Expected the field `filename` to be a primitive type in the JSON string but got " + data['filename']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['blob_url'] && !(typeof data['blob_url'] === 'string' || data['blob_url'] instanceof String)) {
            throw new Error("Expected the field `blob_url` to be a primitive type in the JSON string but got " + data['blob_url']);
        }
        // ensure the json data is a string
        if (data['raw_url'] && !(typeof data['raw_url'] === 'string' || data['raw_url'] instanceof String)) {
            throw new Error("Expected the field `raw_url` to be a primitive type in the JSON string but got " + data['raw_url']);
        }
        // ensure the json data is a string
        if (data['contents_url'] && !(typeof data['contents_url'] === 'string' || data['contents_url'] instanceof String)) {
            throw new Error("Expected the field `contents_url` to be a primitive type in the JSON string but got " + data['contents_url']);
        }
        // ensure the json data is a string
        if (data['patch'] && !(typeof data['patch'] === 'string' || data['patch'] instanceof String)) {
            throw new Error("Expected the field `patch` to be a primitive type in the JSON string but got " + data['patch']);
        }
        // ensure the json data is a string
        if (data['previous_filename'] && !(typeof data['previous_filename'] === 'string' || data['previous_filename'] instanceof String)) {
            throw new Error("Expected the field `previous_filename` to be a primitive type in the JSON string but got " + data['previous_filename']);
        }

        return true;
    }


}

DiffEntry.RequiredProperties = ["sha", "filename", "status", "additions", "deletions", "changes", "blob_url", "raw_url", "contents_url"];

/**
 * @member {String} sha
 */
DiffEntry.prototype['sha'] = undefined;

/**
 * @member {String} filename
 */
DiffEntry.prototype['filename'] = undefined;

/**
 * @member {module:model/DiffEntry.StatusEnum} status
 */
DiffEntry.prototype['status'] = undefined;

/**
 * @member {Number} additions
 */
DiffEntry.prototype['additions'] = undefined;

/**
 * @member {Number} deletions
 */
DiffEntry.prototype['deletions'] = undefined;

/**
 * @member {Number} changes
 */
DiffEntry.prototype['changes'] = undefined;

/**
 * @member {String} blob_url
 */
DiffEntry.prototype['blob_url'] = undefined;

/**
 * @member {String} raw_url
 */
DiffEntry.prototype['raw_url'] = undefined;

/**
 * @member {String} contents_url
 */
DiffEntry.prototype['contents_url'] = undefined;

/**
 * @member {String} patch
 */
DiffEntry.prototype['patch'] = undefined;

/**
 * @member {String} previous_filename
 */
DiffEntry.prototype['previous_filename'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
DiffEntry['StatusEnum'] = {

    /**
     * value: "added"
     * @const
     */
    "added": "added",

    /**
     * value: "removed"
     * @const
     */
    "removed": "removed",

    /**
     * value: "modified"
     * @const
     */
    "modified": "modified",

    /**
     * value: "renamed"
     * @const
     */
    "renamed": "renamed",

    /**
     * value: "copied"
     * @const
     */
    "copied": "copied",

    /**
     * value: "changed"
     * @const
     */
    "changed": "changed",

    /**
     * value: "unchanged"
     * @const
     */
    "unchanged": "unchanged"
};



export default DiffEntry;

