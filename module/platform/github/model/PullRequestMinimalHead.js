/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PullRequestMinimalHeadRepo from './PullRequestMinimalHeadRepo';

/**
 * The PullRequestMinimalHead model module.
 * @module model/PullRequestMinimalHead
 * @version 1.1.4
 */
class PullRequestMinimalHead {
    /**
     * Constructs a new <code>PullRequestMinimalHead</code>.
     * @alias module:model/PullRequestMinimalHead
     * @param ref {String} 
     * @param sha {String} 
     * @param repo {module:model/PullRequestMinimalHeadRepo} 
     */
    constructor(ref, sha, repo) { 
        
        PullRequestMinimalHead.initialize(this, ref, sha, repo);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ref, sha, repo) { 
        obj['ref'] = ref;
        obj['sha'] = sha;
        obj['repo'] = repo;
    }

    /**
     * Constructs a <code>PullRequestMinimalHead</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PullRequestMinimalHead} obj Optional instance to populate.
     * @return {module:model/PullRequestMinimalHead} The populated <code>PullRequestMinimalHead</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PullRequestMinimalHead();

            if (data.hasOwnProperty('ref')) {
                obj['ref'] = ApiClient.convertToType(data['ref'], 'String');
            }
            if (data.hasOwnProperty('sha')) {
                obj['sha'] = ApiClient.convertToType(data['sha'], 'String');
            }
            if (data.hasOwnProperty('repo')) {
                obj['repo'] = PullRequestMinimalHeadRepo.constructFromObject(data['repo']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PullRequestMinimalHead</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PullRequestMinimalHead</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PullRequestMinimalHead.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ref'] && !(typeof data['ref'] === 'string' || data['ref'] instanceof String)) {
            throw new Error("Expected the field `ref` to be a primitive type in the JSON string but got " + data['ref']);
        }
        // ensure the json data is a string
        if (data['sha'] && !(typeof data['sha'] === 'string' || data['sha'] instanceof String)) {
            throw new Error("Expected the field `sha` to be a primitive type in the JSON string but got " + data['sha']);
        }
        // validate the optional field `repo`
        if (data['repo']) { // data not null
          PullRequestMinimalHeadRepo.validateJSON(data['repo']);
        }

        return true;
    }


}

PullRequestMinimalHead.RequiredProperties = ["ref", "sha", "repo"];

/**
 * @member {String} ref
 */
PullRequestMinimalHead.prototype['ref'] = undefined;

/**
 * @member {String} sha
 */
PullRequestMinimalHead.prototype['sha'] = undefined;

/**
 * @member {module:model/PullRequestMinimalHeadRepo} repo
 */
PullRequestMinimalHead.prototype['repo'] = undefined;






export default PullRequestMinimalHead;

