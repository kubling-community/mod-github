/**
 * GitHub v3 REST API
 * GitHub's v3 REST API.
 *
 * The version of the OpenAPI document: 1.1.4
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MarketplaceListingPlan model module.
 * @module model/MarketplaceListingPlan
 * @version 1.1.4
 */
class MarketplaceListingPlan {
    /**
     * Constructs a new <code>MarketplaceListingPlan</code>.
     * Marketplace Listing Plan
     * @alias module:model/MarketplaceListingPlan
     * @param url {String} 
     * @param accountsUrl {String} 
     * @param id {Number} 
     * @param number {Number} 
     * @param name {String} 
     * @param description {String} 
     * @param monthlyPriceInCents {Number} 
     * @param yearlyPriceInCents {Number} 
     * @param priceModel {module:model/MarketplaceListingPlan.PriceModelEnum} 
     * @param hasFreeTrial {Boolean} 
     * @param unitName {String} 
     * @param state {String} 
     * @param bullets {Array.<String>} 
     */
    constructor(url, accountsUrl, id, number, name, description, monthlyPriceInCents, yearlyPriceInCents, priceModel, hasFreeTrial, unitName, state, bullets) { 
        
        MarketplaceListingPlan.initialize(this, url, accountsUrl, id, number, name, description, monthlyPriceInCents, yearlyPriceInCents, priceModel, hasFreeTrial, unitName, state, bullets);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, url, accountsUrl, id, number, name, description, monthlyPriceInCents, yearlyPriceInCents, priceModel, hasFreeTrial, unitName, state, bullets) { 
        obj['url'] = url;
        obj['accounts_url'] = accountsUrl;
        obj['id'] = id;
        obj['number'] = number;
        obj['name'] = name;
        obj['description'] = description;
        obj['monthly_price_in_cents'] = monthlyPriceInCents;
        obj['yearly_price_in_cents'] = yearlyPriceInCents;
        obj['price_model'] = priceModel;
        obj['has_free_trial'] = hasFreeTrial;
        obj['unit_name'] = unitName;
        obj['state'] = state;
        obj['bullets'] = bullets;
    }

    /**
     * Constructs a <code>MarketplaceListingPlan</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MarketplaceListingPlan} obj Optional instance to populate.
     * @return {module:model/MarketplaceListingPlan} The populated <code>MarketplaceListingPlan</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MarketplaceListingPlan();

            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('accounts_url')) {
                obj['accounts_url'] = ApiClient.convertToType(data['accounts_url'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('number')) {
                obj['number'] = ApiClient.convertToType(data['number'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('monthly_price_in_cents')) {
                obj['monthly_price_in_cents'] = ApiClient.convertToType(data['monthly_price_in_cents'], 'Number');
            }
            if (data.hasOwnProperty('yearly_price_in_cents')) {
                obj['yearly_price_in_cents'] = ApiClient.convertToType(data['yearly_price_in_cents'], 'Number');
            }
            if (data.hasOwnProperty('price_model')) {
                obj['price_model'] = ApiClient.convertToType(data['price_model'], 'String');
            }
            if (data.hasOwnProperty('has_free_trial')) {
                obj['has_free_trial'] = ApiClient.convertToType(data['has_free_trial'], 'Boolean');
            }
            if (data.hasOwnProperty('unit_name')) {
                obj['unit_name'] = ApiClient.convertToType(data['unit_name'], 'String');
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], 'String');
            }
            if (data.hasOwnProperty('bullets')) {
                obj['bullets'] = ApiClient.convertToType(data['bullets'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MarketplaceListingPlan</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MarketplaceListingPlan</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MarketplaceListingPlan.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // ensure the json data is a string
        if (data['accounts_url'] && !(typeof data['accounts_url'] === 'string' || data['accounts_url'] instanceof String)) {
            throw new Error("Expected the field `accounts_url` to be a primitive type in the JSON string but got " + data['accounts_url']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['price_model'] && !(typeof data['price_model'] === 'string' || data['price_model'] instanceof String)) {
            throw new Error("Expected the field `price_model` to be a primitive type in the JSON string but got " + data['price_model']);
        }
        // ensure the json data is a string
        if (data['unit_name'] && !(typeof data['unit_name'] === 'string' || data['unit_name'] instanceof String)) {
            throw new Error("Expected the field `unit_name` to be a primitive type in the JSON string but got " + data['unit_name']);
        }
        // ensure the json data is a string
        if (data['state'] && !(typeof data['state'] === 'string' || data['state'] instanceof String)) {
            throw new Error("Expected the field `state` to be a primitive type in the JSON string but got " + data['state']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['bullets'])) {
            throw new Error("Expected the field `bullets` to be an array in the JSON data but got " + data['bullets']);
        }

        return true;
    }


}

MarketplaceListingPlan.RequiredProperties = ["url", "accounts_url", "id", "number", "name", "description", "monthly_price_in_cents", "yearly_price_in_cents", "price_model", "has_free_trial", "unit_name", "state", "bullets"];

/**
 * @member {String} url
 */
MarketplaceListingPlan.prototype['url'] = undefined;

/**
 * @member {String} accounts_url
 */
MarketplaceListingPlan.prototype['accounts_url'] = undefined;

/**
 * @member {Number} id
 */
MarketplaceListingPlan.prototype['id'] = undefined;

/**
 * @member {Number} number
 */
MarketplaceListingPlan.prototype['number'] = undefined;

/**
 * @member {String} name
 */
MarketplaceListingPlan.prototype['name'] = undefined;

/**
 * @member {String} description
 */
MarketplaceListingPlan.prototype['description'] = undefined;

/**
 * @member {Number} monthly_price_in_cents
 */
MarketplaceListingPlan.prototype['monthly_price_in_cents'] = undefined;

/**
 * @member {Number} yearly_price_in_cents
 */
MarketplaceListingPlan.prototype['yearly_price_in_cents'] = undefined;

/**
 * @member {module:model/MarketplaceListingPlan.PriceModelEnum} price_model
 */
MarketplaceListingPlan.prototype['price_model'] = undefined;

/**
 * @member {Boolean} has_free_trial
 */
MarketplaceListingPlan.prototype['has_free_trial'] = undefined;

/**
 * @member {String} unit_name
 */
MarketplaceListingPlan.prototype['unit_name'] = undefined;

/**
 * @member {String} state
 */
MarketplaceListingPlan.prototype['state'] = undefined;

/**
 * @member {Array.<String>} bullets
 */
MarketplaceListingPlan.prototype['bullets'] = undefined;





/**
 * Allowed values for the <code>price_model</code> property.
 * @enum {String}
 * @readonly
 */
MarketplaceListingPlan['PriceModelEnum'] = {

    /**
     * value: "FREE"
     * @const
     */
    "FREE": "FREE",

    /**
     * value: "FLAT_RATE"
     * @const
     */
    "FLAT_RATE": "FLAT_RATE",

    /**
     * value: "PER_UNIT"
     * @const
     */
    "PER_UNIT": "PER_UNIT"
};



export default MarketplaceListingPlan;

